{
  "name": "agentflow-workspace",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "src/apps/*",
    "src/packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "check:boundaries": "node src/tooling/check-boundaries.mjs",
    "typecheck:tests": "tsc -p tsconfig.tests.json",
    "test": "npm run build && npm run typecheck:tests && node src/tooling/run-tests.mjs",
    "check": "npm run check:boundaries && npm run studio:typecheck && npm test",
    "demo": "npm run build && node src/apps/cli/dist/index.js demo",
    "studio": "npm run build && node src/apps/studio/dist/server.js",
    "studio:build": "npm run build && tsc -p src/apps/studio/client/tsconfig.json && vite build --config src/apps/studio/vite.config.mts",
    "studio:typecheck": "npm run build && tsc -p src/apps/studio/client/tsconfig.json && tsc -p tsconfig.browser.json",
    "studio:test": "npm run studio:build && playwright test --project=journeys",
    "check:quality": "npm run check:boundaries && npm run studio:typecheck && npm run typecheck:tests",
    "test:unit": "node src/tooling/run-tests.mjs --group unit",
    "test:integration": "node src/tooling/run-tests.mjs --group integration",
    "test:workflows": "node src/tooling/run-tests.mjs --group workflows",
    "studio:visual": "node src/tooling/ci/visual.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "26.5.0",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "tsx": "4.23.13",
    "typescript": "5.9.3"
  }
}
