# 同一 NodeTask 的有限重试与持久等待

创建于 2026-09-10。

## 结论与边界

节点可声明最大尝试次数、允许错误类别和固定间隔，次数包含首次执行。无策略时不自动重试已失败执行；#13 的显式中断恢复仍可调用，已有恢复历史不丢失。有策略的节点，其恢复新 Attempt 同样受该 NodeTask 的累计预算限制，不重新开始局部计数循环。业务 outcome 和返修路由保持 #9 的正常语义，重入节点产生新的 NodeTask。

首期类别对齐可信宿主结构化错误码：timeout、execution_failure、interrupted。只映射当前确切的执行码，不解析 Agent 自由文本，不把契约错误、取消、认证配置错误或未知 Effect 结果归入临时错误。配置拒绝未知类别和无效参数；以后增加可信错误接口时再扩展映射。 文件 Workflow Catalog 依据实际 Runner 的 timed_out 事实统一返回 EXECUTION_TIMEOUT；这项错误码规范化也适用于未配置重试的普通调用。此前 Script / Agent 分别可能返回 SCRIPT_EXECUTION_FAILED / EXECUTION_NOT_SUCCESSFUL，其他失败码及默认一次执行保持不变。错误事实归一化留在执行适配层，策略仍只消费结构化码，不向 Adapter 或 Runner 注入重试配置。

Attempt 开始前的持久写入登记身份；源等待与未成功领取不进入 Attempt。启动不确定仍先按 #13 核对，不能凭未见成功就退还预算。重试失败结果、类别及原始决定/到期时间属于同一 Run 的 Attempt 历史，原定输入保持在正常游标，失败输出不接纳。等待状态与队列索引在同一条件提交中保存；等待不占 Worker 或共享执行容量，到期仍受角色和认证限制。

重试判断保持纯函数。正常引擎负责验证、确认失败资源清理及记录等待，队列只消费已保存的到期时间；不在 Worker 内睡眠或另建结果数据库。首期自动重试要求持久执行接口。Effect 的可靠回执和未知操作核对继续走既有端口，未知结果绝不因错误类别匹配而重发。

## 方案考量（alternatives）

| 方案 | 可取之处 | 代价或不适用原因 | 取舍 |
| --- | --- | --- | --- |
| 按持久 Attempt 历史计算预算，并记录等待决定 | 恢复不能重置次数，可检查每次失败和输入 | 需要严格加载校验与正常执行/队列共同接线 | 采用 |
| 复制旧局部重试循环 | 实现短，旧行为可参考 | 重开会获得新的循环次数，等待长期占 Worker | 不采用 |
| 外置策略脚本或独立重试任务数据库 | 可以定制复杂策略 | 首期增加双重事实和运维负担 | 不采用 |

## 影响与验证

验证默认一次失败、明确错误类别、同任务新 Attempt、返修新任务、独立输入/失败材料、真实第二次中断后最多第三次、持久等待/取消/容量释放、旧执行核对及 Effect 不盲重发。变更保持当前普通执行与显式恢复接口兼容，有策略的累计上限在正常与恢复路径一致生效。

## 重开条件

需要复杂退避、动态策略、不同执行错误接口、历史迁移或新类型副作用时重新评估；不以首期未覆盖的接口冒充已支持。

## 确认与变更留痕

- 用户已确认默认不自动重试、用户策略、原定输入、等待释放容量、跨重启累计次数和 Effect 边界，并授权依序实施；依据 [Issue #15](https://github.com/TonQiaN/Agent_flow/issues/15)。主责 @xiaoxuanli-a，Codex 协助实施与作者验证，无其他开发者独立审阅。
- 基线 ab9551c。预检先核对 Blackbox v0.1.22 / 5610d1b 的 runtime.py 节点重试循环及 scheduler.py / storage.py 基础设施等待；不混用两层计数。本文登记常规接口取舍，不冒充新一轮用户逐字段确认。

## 首版落实

- 2026-09-10：节点 retry 字段、纯判断、正常执行与恢复累计预算、可验证的失败历史、持久 nextAt、队列释放/重新领取及等待取消已实现。沿用 v5 检查点的可选字段，不提供历史迁移工具；当前最大尝试次数为 100，固定间隔最多一天。
- 认证旧领取令牌在新领取准入时封存，等待只保留当前令牌供下次核对，避免已封存历史累积成为额外重试次数限制。
- 实际普通测试 385 项与新增真实 Docker/Agent 重试组合 3 项通过；完整证据见 [作者验收](../../../../docs/validation/2026-09-10-node-retries.md)。本地作者验收不等于 PR 合入或独立审阅，完整交付仍未完成，保留 proposed。
