# Issue 领域与结果表达改版验证

日期：2026-09-10。关联 [Issue #21](https://github.com/TonQiaN/Agent_flow/issues/21)，主负责开发者 TonQiaN，Codex 实施与自查；未进行多人审阅。

## 对象与基线

基线为 `origin/main@f28540c6bc3cda2f422fc10a1bca27b0df9e36ba`，已 fetch 核对。工作分支 `codex/issue-21-domain-field` 在五份完整 Issue 表单中新增 `domain`，并根据后续审查反馈去除预设方法与方案输入，配套修订 [分类决定](../../.agents/decisions/development/README.md#d-20260908-issue-classification)、[共通规范](../../.agents/decisions/development/README.md#d-20260908-issue-template-common)、相关类别决定与填写指南。

9 月 8 日的八字段演练与线上验证保留为历史。本轮用户先要求增加领域，随后审查 #20 时明确 Issue 应说清当前情况与想要的结果，不应预设限制实现者及 AI 发挥的方法，并要求将该改动一并纳入 #21。下面分开记录两次范围对应的检查，旧检查不替代最终改版结果。

## 首轮仅新增领域的检查

在 macOS 上使用 Ruby 的 Psych/YAML 解析五份表单，检查重复 YAML key、字段 ID、顺序、共同对象及原有定义。临时检查脚本不作为项目生成器或 CI 发布。

| 检查 | 结果 |
| --- | --- |
| 五表 YAML 解析、mapping key 与字段 ID 唯一 | 通过 |
| `domain` 位于第一个输入；dropdown；选项依次为开发流程、项目内容 | 五表一致 |
| `multiple: false`、不含 default、`required: true` | 五表一致；required 是填写意图，见平台边界 |
| 九项共通职责，除 problem 外八项完整对象相同 | 通过 |
| 去除新增 domain 后，与各自基线 YAML 完整对象比较 | 全部相等；原字段、提示、必填意图、专用块及表单元信息均保留 |
| 输入块数量 | 功能 10、缺陷 11、研究 11、决策 11、维护 10 |
| 正式 Markdown 中的相对文件链接与标题锚点 | 检查 216 个引用，均可解析；未扫描 materials |
| `git diff --check` | 通过 |

## 领域填写与边界核对

以下是首轮对分类规则和字段可表达性的自查，不表示执行了示例中的产品工作。当时只为真实模板任务创建 #21，未创建演练 Issue或回写其他正文；后续 #20/#21 的结果表达修订见下节。

| 场景 | 所属领域 / 主要类型 | 填写判断 |
| --- | --- | --- |
| #20：研究本仓库 AGENTS.md 的设计与协作效果 | 开发流程 / 研究 | 对象是团队开发方法；研究不被默认归到产品 |
| #21：补充 Issue 共通分类字段 | 开发流程 / 维护 | 交付模板及配套说明；已通过 CLI 建项并读回“所属领域：开发流程”和真实 Assignees |
| #10：实现产品使用的 Harness Adapter | 项目内容 / 功能 | 作用对象是产品运行能力，即使涉及 Harness 配置也不归开发指令 |
| 假设产品运行记录违反既有输出契约 | 项目内容 / 缺陷 | 主类按恢复契约确定；没有执行故障复现 |
| 验证产品中节点插件的隔离能力 | 项目内容 / 研究 | 与 #20 同为研究，领域不同；没有执行隔离实验 |
| 敲定 AgentFlow 首版产品范围，只交正式决定 | 项目内容 / 决策 | 是否写产品代码不决定领域；决定采纳不代表实现 |
| 修订产品使用指南 | 项目内容 / 维护 | 与 #21 同为维护及文档产物，按说明对象区分 |
| 实现 Runner 能力，同时补开发者调试说明 | 项目内容 / 功能 | 按主要验收结果选领域，在 scope 说明附带影响；不因跨领域自动拆项 |

两个领域各有独立可验收结果时，沿用既有拆项判断；无法确定主要对象时先澄清范围，不填两个选项或预设“混合”。CLI/API 正文通过“所属领域”小节保存选项文字，新字段没有标签或流程自动化。

## 加入结果表达后的最终本地检查

重新解析最终五表并核对：新增 domain 和六个删除的专用 ID 均与迁移决定对应，没有额外字段丢失；同义共同 ID 保留，除 problem 外八项完整对象在五表一致。领域的两个选项、单选、无默认值及必填意图保持；表单名称、标题前缀、owner 和交付物选项保留。缺陷的 problem、reproduction、environment 完整对象与基线相等。

最终输入块为功能 9、缺陷 11、研究 9、决策 9、维护 9。移除的是 behavior_contract、evidence_plan、stop_rule、options、impact、change_plan；已知事实与期望行为的去向见 [迁移说明](../development/issue-templates.md#完成与迁移)。验收标题统一为“完成后应看到的结果”，范围和续接提示也不再要求计划。

另外填写五个完整的目标示例，分别为外围回放、假设回放缺陷、AGENTS.md 研究、首版范围选择、模板改进。每个示例覆盖当前全部字段，均能在不提供实现/研究路线的情况下表达情况、目标和必要约束。此处只验证可填写性，不运行示例产品或研究，不证明长期使用效果。

最终检查 215 个正式 Markdown 相对文件引用及标题锚点，均可解析；七个模板相关决定 ID 各有一份正文。`git diff --check` 通过。再次 fetch 后基线仍与 origin/main 一致。当前没有项目 CI 工作流，本次记录的是局部检查与作者自查。

#20 已从预设研究步骤整理为现状、关键问题、期望成果及结论质量，正文由 4590 字符缩至 2099 字符；这只是表达修订，不表示研究完成。#21 的标题、范围与验收同时包含领域分类和结果表达两项改动。两项服务器正文均与准备的文本逐字读回一致，主责仍为 TonQiaN，保持 OPEN；未改写其他 Issue。

## 平台与交付边界

2026-09-10 重新核对 [GitHub 表单结构说明](https://docs.github.com/en/communities/using-templates-to-encourage-useful-issues-and-pull-requests/syntax-for-githubs-form-schema) 中 dropdown 的 options、multiple、default 与 required。官方仍将 required 的提交拦截注明为仅适用于公开仓库；本仓库私有，按人参与的就绪核对补齐信息。字段文字不会自动设置 Labels 或 Assignees。没有以空白 Issue 试探提交拦截。

尚未合入默认分支，未验证新版 GitHub 五类实际表单中的展示、选择及提交后保存。本地解析及 #20/#21 的 CLI 正文读回不代替网页验收。分类、共通和发生专用字段改选的功能/研究/决策/维护决定移回 proposed；缺陷专用事实字段保持原定义，通用表达随共通决定衔接。

合并后需核对默认分支提交及五类表单中的领域选项、默认状态和保留字段，再记录实际结果；此前保持 Issue #21 开放，不把旧版线上验证或未执行的产品测试计为通过。
