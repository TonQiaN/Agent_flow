# Issue #39：本机工作台整体验证

2026-09-15，macOS、Node 24.18.1、Docker Desktop。主负责人 @TonQiaN；Codex（Astra）实施、真实浏览器操作和作者自查，尚未独立人类审阅。第三层基于 `3e32960`（同步 main `c8e1890` 的版本交接文档），前置 PR #40 → #41；本记录不表示已经合并或发布。

## 2026-09-17：合并前评审修正

用户已明确授权合并这组 PR，并在逐项核对后决定是否关闭 #39。Codex 复核未解决的自动评审意见和实际代码，补齐：同毫秒父子修订按全局序号对齐、日志翻页与刷新保持连续、切换运行忽略旧请求、回看较早修订不残留后一次执行选择、批卷 JSON 的前后端校验。当前定义共用公开投影，排除私有恢复与凭据配置；结束文件先于快照读取，避免一次请求混合旧状态和新结束标记。

- 质量检查通过；服务、上传、招聘契约与 SQLite 回放 19/19。包含同毫秒父子运行的 API 与存储回归。
- 真实浏览器新增 400 条日志翻页后刷新、延迟请求跨运行、批卷无效材料拒绝；返工旅程追加第二次执行回放到第一次输出。初次新增返工断言误将历史 steps 数组当作数量，修正测试后该旅程通过；没有放宽输出内容断言。
- #41 首轮远端 Node 24 的 Fork 身份回归读到 queued，同一 head 仅重跑失败任务通过，首次证据保留。此次改善结束文件/快照读取顺序并补 worker.log 诊断；首次失败日志不足以证实根因，不把重跑称作修复。
- 沿用 Blackbox `4dc0f4ac630d6e54036dcbfafa1a4ae7ce2fa345` 的配置/时间边界参考；未找到相同前端分页和回放实现。本次使用隔离数据与合成素材，保留的真实招聘运行不改写。

完整 Chromium 13/13（57.7 秒），固定 Linux 环境截图比较 4/4 场景、12 张基准全部通过，基准 PNG 未改；最后补齐实际拖放后上传专项 1/1。结束读取调整后的服务测试 5/5，质量检查通过。当前提交的远端证据集中在 PR #42。

## 2026-09-17：时间、通用产物与页面整体复查

根据用户的连续反馈，本轮沿用画布为主、横向曲线、点击查看详情、流程与节点分清、从流程直达运行的交互。补齐运行和每次节点尝试的开始、结束、耗时，回放增加所选时间、起止时间、经过多久及记录位置；步骤表可直接跳回节点。容器实际执行时间与节点保存记录分别展示，不把准备和清理时间当成纯容器执行时间；旧记录缺失时不倒推。

结果页改为“结果与产物”。招聘保留逐项证据；其他工作流使用结构化字段、表格或原始输出，并可打开各节点的产物。文件按运行、节点和尝试归属分组，同名文件保留各自来源，支持搜索、筛选和跳回节点。PDF 不依赖招聘模板；Python 等源码只读预览，未知格式可下载。文本预览限制 256 KiB，完整下载不截断，空文件单独标明。桌面文件列表与预览区各自滚动；无子任务时直接显示执行步骤。

- `npm run check`：451 通过、196 个需显式环境的用例跳过、零失败；包含前端和浏览器测试类型检查。
- Chromium 完整旅程 11/11（最终复测 54.6 秒）。文件区域滚动调整后，通用产物和完整招聘旅程再次 2/2（15.6 秒）；最终通用输出标题和子任务时间核对 2/2（7.1 秒）。新通用测试使用独立的两节点宿主 Workflow 与真实归档，覆盖 Python 下载、非招聘双页 PDF、两个同名 JSON、源码不执行、空文件、大文件截断、来源跳转和 731px 页面；不使用真实模型，也不写入主历史。
- 新增时间投影测试区分重试和返工，验证缺失时间、时钟倒退、历史截止点；API 测试核对修订边界、文件身份、原始上传时间过滤、下载及预览长度。原有返工和重试浏览器检查同时核对毫秒级时间与尝试先后。
- 实际网页检查最新真实招聘记录：读取 477 ms、匹配 32.1 s、复核 45.7 s、报告 815 ms；匹配的真实容器执行区间为 14:24:25.559–14:24:56.913（31.4 s），与包含准备/清理的节点区间分开。四个节点均有记录可供展示。
- 首轮通用测试发现夹具漏声明 HTML 类型，修正后通过。第一次完整浏览器检查 9/11，两个失败为旧用例仍要求无子运行时显示“主工作流”并行卡片；按本轮界面要求改为断言真实步骤、时间和重试身份后完整通过。原始失败日志与 trace 保留。类型检查还捕获测试导入扩展名和事件 JSON 类型问题，均修复，未放宽编译规则。
- 视觉复查发现时间格式最初只显示毫秒尾数，已指定完整年月日时分秒并增加断言；同时修正不同尝试日志混合、空文件永久加载、文件列表撑长整页；运行中和回放未结束时显示截至所选记录的已用时间，子运行使用自己的起止时间。前述最终检查包含这些修正。
- 主历史仍仅保留 `run-b9677003-47c8-4158-a46d-614573a73e78`；29 个文件与改动前 SHA-256 完全相同。没有重跑模型或改写该运行。

使用习惯总结、点击与截图报告仅在本机私有保存。Blackbox 参考同一提交 `4dc0f4ac630d6e54036dcbfafa1a4ae7ce2fa345` 的 `execution_timeline.py`，区分宿主/等待/执行时间；本项目按自己的修订和 Runner 事件只读投影。远端新提交检查在 PR #42 核对；作者/AI 自查不代替用户复看或独立审阅。

## 2026-09-17：节点任务与配置展示

用户在流程定义页查看 parse 设置时看到“未记录容器配置”。实际最新运行的四个节点均保存了执行绑定；定义页没有把配置传入详情面板，Prompt 也只在原始设置 JSON 中。此次补齐当前定义的任务摘要、完整 Prompt/脚本命令、输入输出要求与容器配置，历史页继续读取原记录。宿主函数、合成执行与上传后生成的命令明确区分；临时检查目录不会显示成本次运行目录。

- Node 24.18.1：`npm run check` 448 通过、196 项环境用例显式跳过、零失败；前端构建与浏览器类型检查通过。
- Chromium 完整用户旅程 **10/10**（52.5 秒），最终任务/输入输出/报告/回放专项 **2/2**（15.3 秒）。新增只读节点检查、完整 Prompt 复制、脚本命令、容器参数、窄屏和切换标签滚动复位；旧 Prompt 与当前不同、旧记录缺少 Prompt 两种情况均确认不会借用当前定义。
- 首轮 9/10，唯一失败是文件缺失测试仍读取固定 `.local/studio-browser-tests`，而本轮使用独立临时数据目录。统一读取 `AGENTFLOW_BROWSER_DATA` 后通过，未放宽缺失文件的业务断言。首次失败 trace 保留在本机报告目录。
- Astra 在本机实际点击 parse/match 的任务、设置、长 Prompt 展开，以及最新真实招聘运行的任务与设置。修复了切换标签后保留旧滚动位置的问题；任务执行容器和版本检查容器分别标注，目录按需展开。
- 主数据目录仍只有 `run-b9677003-47c8-4158-a46d-614573a73e78`；改动前后该目录 29 个文件 SHA-256 全部相同。本轮没有重跑真实模型或改写该次输入、输出、日志与结果。

本轮节点任务的报告和截图仅在本机私有保存。本轮为作者/AI 自查，未代替用户复看或独立审阅。参考 Blackbox `4dc0f4ac630d6e54036dcbfafa1a4ae7ce2fa345` 的 execution_plans.py 对运行配置冻结的处理；未找到对应前端详情实现，本项目沿用自己的只读执行定义和历史绑定。

## 2026-09-16：招聘合并为四节点

用户实测认为十四节点拆分过细，要求 Harness 承担完整逻辑，合并后重新运行并展示输入/输出。现改为读取材料、匹配与推荐、独立复核、生成报告四节点；正常路径仅两次模型 Harness 执行。岗位拆解、逐项分析和二元推荐合并在匹配节点，确定性校验在复核节点完成；不再创建分配、汇总、独立裁决等中间节点。返工最多两轮；旧历史仍按原十四节点读取。

从实际网页一次上传同一合成岗位及陈晨、林晓、周宁三份虚构简历，启动 `run-b9677003-47c8-4158-a46d-614573a73e78`。真实 DeepSeek / `deepseek-flash` 容器完成 `parse → match → audit → render`，`succeeded / completed`：4 步、2 次 Harness、0 次返工、19 个文件条目。4 条岗位要求、3 人各 4 条结论；陈晨推荐通过，林晓因明确缺少 Node.js 与自动化测试实践推荐不通过，周宁因 TypeScript 未证实与接口/测试经历冲突推荐不通过。独立复核三人均无分析错误；宿主逐条核对原文/归属、完整人数与二元结论。3 份个人 PDF、对照表和 JSON 使用同一结果。

本机私有资料区保存上传原件、逐节点运行结果、INPUT/OUTPUT 的 Markdown 与 JSON、可展开证据的输入/输出页面，以及本轮操作截图。材料副本与实际上传件 SHA-256 一致。点击匹配节点可查看整个批次输入/输出；输出先显示本节点生成的字段，完整传递数据可展开，避免重复原始材料盖住结果。结果页按人展示证据与 PDF。完整使用报告也仅在本机私有保存，原始资料不入 Git。

验证包括：

- 批次校验覆盖漏人、重复或未知 ID、串用材料、非法岗位引用、评分字段、必需项无支持却给通过，以及复核漏人/发现问题时不放行。
- JSON 文件适配支持宿主选择已登记 outcome；未知 outcome 失败并保留未接纳草稿，模型或输入中的 outcome 不能覆盖宿主选择。
- 真实队列/断网解析/PDF 的七个合成场景通过：正常、候选人证据返工、岗位证据返工、技术重试、失败、取消、持续错误耗尽返工。正常恰好四步且没有子运行；持续错误在三次匹配后结束，不进入报告节点。
- 默认 `npm run check` 为 448 通过、196 个需显式环境的跳过项。完整 Chromium 9/9 通过（3.0 分钟），验证整批输入/输出、两轮结果、耗尽返工、PDF、历史和回放；并行子任务导航与等待回放移到现有 Map 示例。CI 成功报告新增实际输入/输出 JSON 附件。
- 输出面板调整后，重新构建前端，并复测上传、节点输入/输出、完整数据展开、报告/PDF、回放、窄屏及返工两条旅程：2/2 通过（1.1 分钟）。Astra 刷新真实运行，看到匹配的新生成字段，重新截图；另在输入/输出报告展开周宁的相互矛盾原文，核对两个引用均来自本人第 1 页。
- 第一轮浏览器 7/8 通过；唯一失败为旧的“末节点起点必须在屏外”断言。精简后末节点有一小部分进入首屏，实际仍需横向拖拽；断言改为节点右边界超出画布，保留字号、无重叠、横向、曲线及拖拽检查。专项复测 1/1 通过，首次 trace 保留在本地。

完整浏览器与远端 CI 的最终结果在 PR #42 当前 head 核对；下文各阶段证据不代表十四节点仍是现行结构。本轮仍为作者/AI 自查，未合并或替代用户验收。

## 2026-09-16：追加横向曲线反馈

用户在 731×911 页面要求曲线连接、横向拖拽浏览和线上流转说明。已把折行布局改为从左到右的主路径，结束分支在旁侧展开；初次打开保持 100% 可读比例，只有手动“适应画布”才缩放到全图。连线始终显示实际内容或 outcome 的说明，返工曲线横向标出目的和次数上限，曲线与文字均可点击。增加“回到起点”，保留当前步骤定位和拖动后的布局保存。

新增浏览器检查：主路径不折行且超出首屏；全部 18 条招聘连线有说明，路径是实际弯曲的曲线；拖拽到远处证据关卡、打开详情、返回起点，以及在 731px 窄屏拖到第二步。运行、子任务、回放继续核对原始记录。新拖拽辅助函数曾在 hash 导航刚改变 URL、上一页节点尚未卸载时读取位置，两次复测失败：仅等待同名节点仍会命中旧页面。现改为先确认目标运行标题已出现，再操作它的画布；完整上传、报告、子任务、PDF、回放和窄屏旅程复测通过（1/1，1.2 分钟）。其他六条旅程已在本次曲线代码下通过；默认检查 443 通过、195 个显式环境用例跳过。两份失败 trace 留在本地，未放宽运行结果断言。人工检查另缩小返工曲线的垂直间距，使短画布中的文字更容易看见。

本次完整结果与截图在本地用户报告的“横向曲线”章节；CI 检查在 PR #42 对应提交中核对。下面的重设计和首版记录保留各自阶段的实际证据，不代表旧折行布局仍是当前行为。

追加检查原有“检查与返工示例”发现：有次数上限的路线也可能向前进入分支，统一绕到顶部会经过同列的成功结束节点。已按实际方向和行位置选择曲线端口，修订分支返回也避开结束节点；补充“修订完成”的中文说明。浏览器对两条分支曲线沿途采样，确认不穿过结束节点，并保存原有流程截图；目录/横向画布专项复测 1/1 通过（8.9 秒）。

## 2026-09-16：用户反馈后的重设计

用户实测指出首版目录混淆业务流程和技术示例、画布难以阅读、完整运行入口难找、空详情和说明文字占位。此前作者检查侧重功能可点击和数据正确，没有验证首次理解与发现路径；下文的 2026-09-15 功能证据不能作为首版体验已获认可的依据。

本轮重做目录与工作区：5 个业务工作流和 4 个技术示例分开，每项保留真实定义、稳定 ID、节点数量与主路径预览。画布按实际连接排序编号，补方向箭头、完成/当前/未执行状态、返工路线和定位；删除空详情占位和教学条，点击后才显示节点/连线信息。流程页直接进入所属完整运行，运行页能返回工作流。并行节点从当前或指定轮次的真实 `Attempt.parallel.children` 进入子任务，并返回原轮次。

新增验收包含：

- 从目录选择招聘流程并找到完整运行，无需输入 Run ID 或猜路由；同类批卷以不同名称和 ID 区分。
- 1453×874 默认视口中节点标题实际渲染字号至少 12px、节点不重叠、没有空详情面板；390px 窄屏能直接看到开始节点并打开详情。短屏保持可读的开始位置，完整图可平移或使用“适应画布”。
- 最近真实 DeepSeek 招聘记录 `run-f8f70874-8c69-4717-9b35-895896946f26` 的 14 个不同节点、21 次执行、23 个子任务可追溯；第一轮 12 个评审任务与返工轮 4 个任务分别查看。
- 回放到并行等待时，从未完成的 Attempt 读取真实身份；当时尚未保存快照的子任务明确标注不可查看，不混入未来记录。
- Astra 再次通过页面上传一个岗位、三份合成简历，独立合成环境运行 `run-c5f08d70-5f0e-4ff2-b595-532f5cc1e5e4` 完成 14 步、18 个子任务，进程无错误。此次模型响应为显式夹具，真实执行队列、容器解析、关卡、归档和 PDF；未将其写为新的真模型验收。
- 保留结果/PDF、容器设置、历史、文件缺失、重试、返工、失败、取消与断线回归。Chromium 7/7（3.3 分钟），包含画布短屏取景、子任务轮次、PDF 跳转顶部与截图证据；默认检查 443 通过、195 个环境用例跳过。当前远端结果以 PR Checks 为准。

首次新回归发现自由拖动后把节点放到相邻节点上，会遮住两者之间的短连线；测试增加刷新后布局保留及“自动布局”恢复，再验证可见连线点击。另发现并行等待清空 currentIdentity 后详情缺少子任务，已从未结束 Attempt 的保存身份修正。视觉检查修正搜索框挤压标题、底部工具条遮节点、未走结束分支误用完成图标、切换子任务后未重新取景，以及从结果页下方打开 PDF 时沿用旧滚动位置。

CI 使用相同桌面视口，成功时也保留目录、定义画布、完整运行截图与 HTML 报告；失败仍保存 trace 和 worker 日志。作者截图与完整使用报告仅在本机私有保存；首版报告增加更正说明。参考 [Dify 官方工作流截图](https://dify.ai/_astro/workflow.Dl7EY_X1_rPiFq.webp)、[ComfyUI 界面说明](https://docs.comfy.org/interface/overview)及作者公开截图，未复制应用代码或图像素材。Blackbox 同一已核对提交中未找到相应前端/画布实现，不能替代此次用户路径复查。

最终逐项复查还发现合成响应中，林晓第一项虽为“已支持”，理由却套用了“缺少实践”。已修正测试夹具，使理由与该项状态对应；不改写已有归档。修正后上传、画布、证据、PDF、回放和窄屏完整旅程再次通过（1/1，1.1 分钟），并从页面重新提交同一组合复查。

远端 `2b6a789` 的 PR 检查全部通过，分支检查的浏览器返工用例则连续两次在读进度时超过 15 秒；两份首次失败证据保留，同次运行进程无错误结束。Trace 中完整状态响应已达约 3 MB，测试每秒额外读取完整状态，页面自身又每 1.5 秒读取，形成重复负载。浏览器验收改为观察页面实际显示的结束状态，结束后再单次读取保存的状态，继续核对进程错误和真实引擎结果；未增大时限、未跳过返工或重试失败断言。完成截图也等待文件计数加载。消除了已确认的重复读取，是否解决远端超时以新提交的完整 CI 为准，不将重跑本身写成根因修复。

本轮仍为作者/AI 检查，等待用户复看和独立审阅；不自行宣称用户已经接受新版体验。PR #42 承载这次修订，#40 → #41 → #42 的依赖关系不变，尚未合并。

## 2026-09-15：真实招聘运行

从浏览器同一个上传弹窗提交一个岗位和三份合成简历，使用用户授权的 DeepSeek 测试凭据、`deepseek-flash` 与真实隔离容器。

- Run：`run-f8f70874-8c69-4717-9b35-895896946f26`。
- 结果：`succeeded / completed`，21 个主流程步骤、23 个子运行、6 项岗位要求、41 个文件条目。
- 陈晨（合成）“推荐通过”；林晓（合成）、周宁（合成）“推荐不通过”。无总分、加权分或排名。
- 独立复核发现周宁的 TypeScript“未写明”被错误写成“不支持”，证据关卡先 `revise`，修正为“未证实”后 `passed`；修正前后步骤和材料均保留。未知/冲突对最终推荐的影响有明确说明。
- 下载并按 SHA256 核对 11 份最终报告文件。`reports/results.json` 与引擎最终输出完全一致；三份 PDF 的姓名、二元结论及全部引用原文与 JSON 相符。Astra 在页面实际查看了中文 PDF，并核对分页后的文字。

PDF 摘要：

| 文件 | 字节 | SHA256 |
| --- | ---: | --- |
| c1.pdf | 82588 | `85a076551b224bdd176c64f1fc93406ab77f008ae2ac82c5032f5ae885c03239` |
| c2.pdf | 62933 | `6c76d8e7045c24955a479f1f5279f3e3a33ccd0c2f55d307506c7fa6d5c4025d` |
| c3.pdf | 75500 | `f8e7b5a37d33ca5b5c436a89482f06146484f0ce4c965a658c4b600d50a9d2c5` |

## 验收对应

| 要求 | 已执行的验证 |
| --- | --- |
| 画布、拖动、节点与连线 | 浏览器拖动并核对位置；点击节点各页签和连线，显示实际输出契约、方向与返工限制；仅显示位置改变 |
| 一次上传与启动 | 真实招聘 4 份材料一次提交；浏览器缺材料反例、材料归属、进度、重复请求幂等测试 |
| 容器与历史设置 | 实际镜像摘要、CPU、内存、网络、挂载与完整绑定；历史只用保存的定义，非容器节点明确说明 |
| 现有入口 | 9 个登记入口可查看；原检查返工从页面完成，原 Map/Fork CLI 运行出现在历史；DeepSeek Agent 批卷及持久化批卷实际完成 |
| 并行、等待、返工、重试 | 实际引擎/队列六场景：正常、候选人返工、岗位返工、重试、失败、取消；不同任务与 Attempt 身份可区分 |
| 结果与文件 | 三人对照、逐项证据、个人 PDF；文本、JSON、图片、PDF 预览及下载；不可用归档保留明确占位 |
| 历史与回放 | 工作流/状态/本机日期筛选；分页、播放/暂停/逐步/滑块；起点无未来子任务和结果；读取前后 revision 不变 |
| 数据继续保留 | 实际服务重启后重新打开真实记录和文件；原 CLI 历史可发现；SQLite 重开及迁移测试 |
| 缺失和连接错误 | 无符合条件记录、服务断线/重连、缺失归档、未知旧时间、损坏数据反例；不补造数据 |
| Codex 过程 | 下述真实容器/原生模型/会话与页面展开核对 |
| 用户体验报告 | Astra 实际点击、跳转、拖动、截图并检查；发现问题已修正和复查。报告及截图仅在本机私有保存 |
| 本机使用说明 | [部署指南](../guides/local-studio.md)列出环境、认证、全部入口、材料和使用步骤 |

## 原入口与 Codex

原 DeepSeek Agent 批卷 `run-5a4d2766-f395-445b-bafc-3215db00da83`、持久化批卷 `run-b04f9f40-eb8d-4f1a-b091-c31dec7de125` 均 `succeeded / published`；发布是本机 SimulatedEffectService。页面返工 `run-c3275a65-18d8-4348-9be2-76f27eca2ca6` 完成 5 步。原 CLI Map 与 Fork 分别留下 `cli-6d9ebe53-27cb-4faa-a325-0b0cb1c1da7c`、`cli-b830bc4e-b95d-4657-953b-06f114a66628`，子运行与顺序保留。

Codex 0.153.4 + `gpt-6-astra` 在真实容器执行原批卷工作流：`run-9528ce7e-8b0f-476e-b171-fcd20d380fbb`，5 步，`succeeded / published`。实际网页展开 `command_execution` 事件；保存了 15 条 stdout、1 条 stderr、session 和 capture。会话 envelope：`complete=true`、`truncated=false`、`error=null`、1 份 records、`memory=disabled`。另一个最小工具探针完成两次文件工具操作，保存的会话记录包含消息、工具调用及返回。未出现的思考摘要不补造，隐藏完整 CoT 不承诺可得。

本机 VPN/DNS 把 Codex 目的域名返回保留网段，原 CONNECT 代理按规则拒绝。上述 Codex 真模型验证使用本地测试代理镜像：仅对已允许的 `chatgpt.com`/`auth.openai.com` 通过 DNS-over-HTTPS 取得公网地址，继续执行既有目标和公网校验。未更改宿主 VPN 或放宽隔离规则，该测试镜像未随代码提交。团队遇到相同环境问题应先修复 DNS。DeepSeek 真实招聘使用默认受控代理成功。

## 检查与修正

- `npm run check`：426 个普通测试、17 个 E2E 通过，195 个需要显式环境开关的用例默认跳过；共 443 通过、0 失败。
- 招聘实际 Docker 矩阵 6/6，通过时间 174.4 秒。只替换模型响应，队列、子进程、文件、解析、关卡和 PDF 均真实执行。
- 离线格式测试 1/1：MD、TXT、DOCX、双页文本 PDF、PNG/JPG/JPEG 和扫描 PDF，核对文字、分页与 OCR 来源。
- Chromium 完整旅程、四个故障场景和断线重新查询历史共 6/6，通过时间 2.5 分钟；远端结果以 PR 当前检查为准。
- 新增直接修订/时间查询测试，验证相同时间的保存顺序、未知时间排除、边界与摘要损坏拒绝。相同真实运行第 50 条回放查询从约 1.84 秒降至约 0.17 秒；单次本机测量，不作为固定性能承诺。
- Node 24/26 Docker 矩阵和 Chromium 检查的远端结果以该层 PR 当前 Checks 为准；凭据和真模型任务不进入 CI。

实际体验中修复：模型省略调度身份时错误拒绝、PDF 插件空白与 worker MIME、回放混入未来信息、历史分页被重置、播放时切换任务的记录竞态、失败草稿释放前漏存、缺失归档无提示、长理由挤满对照表、窄屏导航名称、跳转保留旧滚动位置、日期边界使用 UTC、重连未刷新历史、构建产物被边界检查当成源码。修正后执行对应浏览器、存储或 Docker 回归。

## 限制与参考

Tutor 扫描件/报告入口已绑定原应用、上传契约和明确的环境要求；本机未安装外部 Tutor，本轮未重复执行真实 Tutor 学生材料。既有 Tutor 专项记录保留其原范围，不能用招聘验证替代。文档最多 60 页；DOCX/TXT/MD 的第 1 页为逻辑全文，OCR 需对照原图。首版只支持本机、启动和查看，没有设置编辑、取消/恢复页面或共享服务。

参考 Blackbox `xiaoxuanli-a/Agent_workflow` 提交 `4dc0f4ac630d6e54036dcbfafa1a4ae7ce2fa345` 的镜像/代理对应实现，已区分其 Python 代理与当前 Node 代理。前端使用 [React Flow 官方 API](https://reactflow.dev/api-reference/react-flow) 与 [PDF.js canvas 示例](https://mozilla.github.io/pdf.js/examples/)，未复制 ComfyUI/Dify 应用代码。原始截图、下载报告和私有凭据不进入 Git。

远端首次干净安装发现 `studio:typecheck` 先于工作区声明构建，导致找不到 integrations 类型。已将依赖构建放入该命令的前置步骤，使用不含 node_modules/dist 的独立源码副本复查安装与完整检查；不靠放宽 TypeScript 规则消除错误。

最终补查还复现了日志归档回调失败后临时输出仍残留的问题。修复前 cleanup 后有两个文件快照，修复后只保留调用者持有的输入；Agent、脚本和无效脚本输出三种路径均返回明确记录失败，回调只调用一次，重复清理安全。对应文件 Workflow 测试 15/15 通过。Blackbox 同版本的 collector/artifacts 未提供当前 TypeScript 观察回调对应方案，本轮按当前资源所有权规则修复。

Linux CI 暴露两项环境问题：夹具容器生成 root 所有的目录导致测试清理 EACCES，已用宿主 UID/GID 生成；招聘宿主自设 3 秒租约在浏览器并发读取下报 QUEUE_CLAIM_LOST，已恢复引擎默认 30 秒租约，仍按原心跳与过期规则执行。浏览器测试同时检查进程完成错误并保留合成运行的 worker.log/completion.json，避免把进程错误拖成轮询超时。

浏览器 CI 随后捕获 APIRequestContext 读取进度时的 ECONNRESET；对应运行进程的 completion.json 均正常结束，流程已经越过此前的租约失败位置。读取 GET 配置最多两次连接重试，保持原业务状态与错误断言，未启用整条测试重试。最终结果按当前 PR 的完整检查核对。
