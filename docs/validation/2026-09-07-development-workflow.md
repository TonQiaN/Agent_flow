# 开发流程文档与模板验证

日期：2026-09-07。工作项：[Issue #2](https://github.com/TonQiaN/Agent_flow/issues/2)。本记录针对开发流程决定、三类 Issue 表单、PR 模板、指令入口和相关说明的本地工作区版本，不代表产品功能或完整流程已验证。

## 范围与方法

- 检查正式 Markdown 的相对链接及锚点、决策 ID 唯一性、生命周期目录与索引、无正文 status 字段，以及两份 AGENTS.md 的唯一书写决定和 CLAUDE.md 符号链接。
- 用本机 Ruby / Psych 解析 YAML，再检查 Issue 表单名称、字段 ID、类型与必需字段结构；核对验收标准及主责字段，未使用需要预设的标签、项目或固定 assignees。
- 检查 PR 模板能交接 Issue、决策实质改动、验收证据、当前验证对象、实际评审及 stack 依赖；人工逐项核对用户七点要求。
- 运行 Git 差异空白检查；核对改动范围，不扫描或修改无关原始资料正文。

## 初稿验证结果（目录迁移前）

本机 Python 3 进行链接、目录与字段检查，Ruby / Psych 3.1.0 解析 YAML；静态检查于 2026-09-07 执行通过。下表保留初稿当时的结果；后续目录调整见本记录末尾。

| 检查对象 | 实际结果 |
| --- | --- |
| 正式 Markdown | 25 份；100 处本地链接与锚点有效 |
| 决策 | 7 个 ID，各有唯一正文和正确分类索引；无正文 status 字段；新流程决定位于 accepted |
| 指令文件 | 2 份 AGENTS.md 均回链到 D-20260907-agents-writing；CLAUDE.md 仍为根 AGENTS.md 的符号链接 |
| YAML | 4 份可解析；3 类表单共 20 个输入字段，名称与各表单内 ID 不重复 |
| Issue 必填结构 | 三类均声明主责、范围、验收和依赖字段必填；Bug 另声明行为差异、复现和环境必填；候选决策允许后续补充 |
| PR 交接结构 | 包含实际 Issue、验收映射、决策实质增量、head / base、验证、实际评审及依赖字段 |
| 旧规则衔接 | 正式规则与指南已移除“是否强制 Issue 另议”和“仅引用即可”的旧入口，原取舍仍在 alternatives 中保留 |
| Git 差异 | 已跟踪文件的 `git diff --check` 通过；新增文本另检查空白格式 |

对照用户七点要求：Issue 起点及责任验收、非平凡决策与每 PR 决策变更、人参与整理、预检、负责开发者文档先行及开发测试、PR / stack 评审修正合并、同 PR 一并提交均有对应段落。CI/CD、矩阵和 Skill 保持后续议题。

GitHub 只新建了文档治理 Issue #2，并回读确认其为 OPEN、Assignees 为 TonQiaN；未把这一登记当作产品流程验证。

## 局限

- 没有运行真实产品场景、Bug 修复或 stacked PR；初稿整理阶段未创建、合并 PR，后续提交与合并结果在 Issue #2 及关联 PR 留痕。Issue #2 只承接本次文档治理工作。
- 没有发布或验收 GitHub 线上表单，没有配置自动预检、CI/CD、测试矩阵、Skill、分支保护或审批人数。
- YAML 可解析及本地结构检查不等于 GitHub 服务端或界面验证通过。私有仓库表单校验限制及人参与核对的方式见 [工作指南](../development/workflow.md)。
- 当前是作者借助 Codex 进行整理与静态检查，未进行多人审阅。新 [开发流程决定](../../.agents/decisions/development/README.md#d-20260907-development-workflow) 尚未全部落实，迁移到 proposed 不表示撤回已有明确确认。

## 同日后续：审查归入 Issue，删除独立目录

按用户后续明确要求，将产品与开发流程的生命周期缩减为 proposed、implemented、archived、rejected，开发流程决定保持原 ID 迁入 proposed。原目录的设计目的和放弃原因保留在生命周期决定的 alternatives，早期验证结果作为历史记录保留。

同步检查对象包括两类目录、索引、模板、两份 AGENTS.md 及其唯一书写决定、工作指南和 PR 模板。Issue 承接针对具体方案与范围的讨论、分工、审查确认；proposed 包含已敲定待落实的记录，不增加等价状态字段。

迁移后于 2026-09-07 再次执行 Python 静态检查与 `git diff --check`，结果通过：

- 25 份正式 Markdown，103 处本地链接与锚点有效；正式文本行尾与空白检查通过。
- 两个分类各有 4 个生命周期目录，无旧审查目录残留；开发流程决定已迁入 proposed，原产品方向候选继续保留在 proposed。
- 7 个决策 ID 保持唯一正文与唯一索引路由，未新增正文生命周期或审批状态字段。
- 两份 AGENTS.md 仍各自只回链同一唯一书写决定，CLAUDE.md 符号链接有效。
- 人工检查现行说明与执行入口：不再将 proposed 一概视为未敲定；审查政策归开发流程中的 Issue 环节，原目录选用与取消理由只在原生命周期决定中保存。

本轮未改变 Issue 表单 YAML，沿用上轮解析结果；PR 模板新增的是具体方案的 Issue 确认依据提示。没有新增自动预检或 CI，也没有运行真实产品场景。

## 同日后续：澄清整理环节的参与方式

用户随后明确应表述为“人参与整理”。已在开发流程决定、唯一指令书写决定、根指令、功能与缺陷表单、PR 模板及指南中同步：AI / 工具协助提炼、拆分和补齐信息，人参与澄清、纠正与关键确认。上文初稿中的简称以本次澄清为准。

本次静态检查通过：4 份 YAML 可解析，3 类表单共 20 个输入字段及既有必填约束保持有效；25 份正式 Markdown 和模板的空白格式检查通过，`git diff --check` 通过。现行规则及入口已统一参与方式的措辞，目录迁移结果仍有效。未运行真实产品场景。

## 提交前复核

2026-09-07，用户明确授权提交、推送、创建 PR 并合并，范围为本次流程决定、正式指引与模板。已获取远端最新引用，检查基线为 origin/main 的 1af884b；本地工作分支起点与该基线一致。

Python 链接、目录、唯一性与文本检查，以及 Ruby / Psych 表单解析均通过：25 份正式 Markdown、103 处本地链接与锚点、7 个唯一决策、两类各 4 个生命周期目录、2 份指令文件、4 份 YAML、3 类表单共 20 个字段。Git 差异空白检查通过。

GitHub 工作流查询返回 0 个工作流，本次没有远端 CI 测试结果。按 Issue #2 中的明确要求进行内容自查及 Codex 辅助检查，未进行其他开发者的独立审阅。实际提交和合并结果留在对应 Issue / PR；真实产品流程仍待后续验证。
