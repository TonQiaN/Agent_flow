# 2026-09-17 CI 分组与诊断

关联 [Issue #39](https://github.com/TonQiaN/Agent_flow/issues/39)，用户明确选择改进 1–4、6。本层覆盖分组、触发、诊断和汇总检查；截图比较由前端层接入。

## 已验证

- 快速质量检查（边界、构建、测试类型）通过；基础层单元测试 422/422。
- 三个 CI 工具测试通过：任务分组无遗漏/重叠；汇总拒绝失败、取消、跳过、缺失；故意超时的子进程返回非零并留下阶段时间、stdout/stderr 与 `testTimeoutFailure` 事件。
- 真实 Docker Runner 恢复 10/10，包括此前偶发超时的同名容器所有权冲突场景；诊断文件显示准备、创建、恢复、查询和清理。本次通过不解释此前超时根因。
- 既有首败：PR #41、`3dd938a`、[run 35184800138 attempt 1](https://github.com/TonQiaN/Agent_flow/actions/runs/35184800138/attempts/1)，Node 24 的所有权冲突测试超过 15 秒。原分支矩阵通过、同 SHA 失败 job 重跑通过。没有调整测试期限、放宽断言或声称已查明原因。

## 验证边界

本地环境 macOS、Node 24.18.1、Docker 29.6.2。远端 Linux/Node 24/26 当前提交结果与分支保护读回须在 PR 交接中记录；本地结果不能替代。没有扩展此前因缺少原生 Harness、网络或真实账号环境而跳过的用例。本次作者/AI 自查，未进行多人独立审阅，也未合并 PR。

## 前端层截图验证

- 固定 Playwright 1.63.0 Noble linux/amd64 镜像（manifest `bc6ab0d6d44ff4826e4cb8c1e6d801e185bfc42bb0753f8e2a30efc70db054c7`），图像在 `src/tests/browser/__screenshots__/visual/`，共 12 张、约 1.2 MiB。所有图已逐张检查；上传弹窗回到顶部，回放点击定位当前步骤，避免把无关滚动位置或屏外节点当作验收画面。
- `npm run studio:visual`：4/4 场景、12/12 基准比较通过（23.8 秒），没有更新基准、没有遮罩时间/节点字段。
- `npm run studio:visual -- --negative-control`：注入 16px 紫色边框，比较按预期失败，检测到 82,832 个不同像素（约 7%），生成 expected / actual / diff、trace 和 HTML 报告。负对照只作用于测试页面，没有写入产品 CSS 或基准。已查看差异图。
- `npm run studio:test`：真实服务 Chromium 用户旅程 11/11（55.2 秒）；涵盖实际上传与运行、节点任务、回放、返工、重试、失败、取消、耗尽、断线重连、Map 和通用产物。质量检查含前端和浏览器测试类型检查通过。
- 首轮基准搭建失败记录：固定容器的 root 身份被现有 Docker 配置校验拒绝，改为普通用户；显示夹具含重试定义却使用非持久入口，被引擎拒绝，改用真实持久入口。没有修改生产校验、放宽测试或使用真实模型密钥。
- 比较、更新和负对照产物分开保存在 `.local/visual-comparison/`、`.local/visual-baseline-update/`、`.local/visual-negative/`。主服务与用户历史未用于测试。截图夹具只验证显示，不能替代真实工作流和模型验收。

远端基础层 `d55decd` 的 [CI](https://github.com/TonQiaN/Agent_flow/actions/runs/35190043863) 和服务层 `0aecc6d` 的 [CI](https://github.com/TonQiaN/Agent_flow/actions/runs/35190045539) 均通过固定汇总门槛。前端层远端 CI 与最终分支保护状态在本 PR 当前交接中续接，避免把旧结果写成新提交的结果。

## 首次远端截图失败与修正

`a898427` 的 [run 35191154009](https://github.com/TonQiaN/Agent_flow/actions/runs/35191154009) 首次截图比较有 1 个场景失败（其余 3 个通过）：PDF 翻页后的预览区滚动位置不同，12,877 个像素有差异。已下载并查看 expected / actual / diff：CI 的文件标题、来源和下载入口滚出预览区，PDF 本文整体上移；不是文字内容变化。测试现在等待第一页和第二页画布都渲染完成，再回到文件标题并断言滚动位置。**原有 12 张基准及容差未改变**。该运行其余任务通过，`CI required` 随截图任务失败，证明汇总不会放行失败项。首败工件与日志保留在 GitHub 及 `.local/ci-review/pr42-visual-first/`，新提交结果在当前 PR 续接。

诊断复查还发现 node:test 在正常完成后也会触发 TestContext.signal；已排除阶段为 done 的情况，避免误记 aborted。真实 Docker 定向复验 1/1，记录止于 done，没有 aborted。

## 合并后并行示例复验

前端 stack 合并到 `8725038` 后，[main run 35198168643](https://github.com/TonQiaN/Agent_flow/actions/runs/35198168643) 的 Node 24/26 单元组均失败：Map 示例报 `Example did not finish`，Node 24 的 Fork 也失败；浏览器、截图、工作流和 Docker 任务通过。另一次 [main run 35196736214](https://github.com/TonQiaN/Agent_flow/actions/runs/35196736214) 记录并行顺序断言实际为 b、a、c，预期 b、c、a。保留这些首败，不把先前同提交重跑成功当作修复。

- Blackbox 参考 commit `4dc0f4ac630d6e54036dcbfafa1a4ae7ce2fa345`：阅读 async-run-queue.md 与 test_parallel_runtime.py；它采用整条工作流租约和线程并行，本项目采用节点租约与 SQLite CAS，未直接复用旧实现。
- 可控复现：在真实示例进程领取任务后阻塞事件循环 350ms，Map/Fork 均得到 `QUEUE_CLAIM_LOST`，继而重现相同的“未完成”错误。旧示例使用 300ms 最小租约且丢弃协调执行结果；CI 旧日志没有记录租约内部原因，因此此复现证明该确定性缺陷，不声称还原远端每次调度过程。
- 修正：示例回到 Worker 默认 30s 租约，继续使用真实时钟与续租。协调及子任务错误都立即检查并报告节点和错误码。引擎到期与隔离机制不变，也没有添加自动重跑。
- 顺序测试改为等三个任务均进入后，依次释放 b、c、a，并等每个 Worker 真正结束；最后仍断言归并输出保持 a、b、c。取消、容量与独立租约到期测试保留。
- 本地定向验证 21/21：包含真实 Studio Map/Fork、并行容量/取消/返工，以及 3 个示例进程回归。新增的 3 项测试在修复前均失败、修复后均通过；首次测试类型检查发现新夹具 env 缺少 ProcessEnv 类型，补齐后重验。全量质量、单元及当前远端 head 结果由补充 PR 记录。

上述修正是 Issue #39 合并后验收的一部分；在最终 main 检查通过前保持 Issue 打开。
