# 2026-09-17 CI 分组与诊断

关联 [Issue #39](https://github.com/TonQiaN/Agent_flow/issues/39)，用户明确选择改进 1–4、6。本层覆盖分组、触发、诊断和汇总检查；截图比较由前端层接入。

## 已验证

- 快速质量检查（边界、构建、测试类型）通过；基础层单元测试 422/422。
- 三个 CI 工具测试通过：任务分组无遗漏/重叠；汇总拒绝失败、取消、跳过、缺失；故意超时的子进程返回非零并留下阶段时间、stdout/stderr 与 `testTimeoutFailure` 事件。
- 真实 Docker Runner 恢复 10/10，包括此前偶发超时的同名容器所有权冲突场景；诊断文件显示准备、创建、恢复、查询和清理。本次通过不解释此前超时根因。
- 既有首败：PR #41、`3dd938a`、[run 35184800138 attempt 1](https://github.com/TonQiaN/Agent_flow/actions/runs/35184800138/attempts/1)，Node 24 的所有权冲突测试超过 15 秒。原分支矩阵通过、同 SHA 失败 job 重跑通过。没有调整测试期限、放宽断言或声称已查明原因。

## 验证边界

本地环境 macOS、Node 24.18.1、Docker 29.6.2。远端 Linux/Node 24/26 当前提交结果与分支保护读回须在 PR 交接中记录；本地结果不能替代。没有扩展此前因缺少原生 Harness、网络或真实账号环境而跳过的用例。本次作者/AI 自查，未进行多人独立审阅，也未合并 PR。
