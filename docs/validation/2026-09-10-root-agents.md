# 根 AGENTS.md 与同目录指令链接验证

2026-09-10，针对 [Issue #20](https://github.com/TonQiaN/Agent_flow/issues/20) 中本轮明确确认的根入口改动。基线为 main `eb108cdb432a08a2e0b96144a2ee30226b9cb9e6`，包含已合并的 PR #24；本轮使用独立工作分支 `codex/issue-20-root-agents`。

## 已确认范围与写入顺序

当前用户明确根目录 AGENTS.md 使用独立决定，要求简短、准确、具体，并包含同目录 CLAUDE.md 符号链接规则、随开发更新的仓库布局、docs 工作入口与团队约定。TonQiaN 主责，Codex 起草、实施与自查。

先建立 [根入口独立决定](../../.agents/decisions/development/README.md#d-20260910-root-agents-writing)，调整 [原书写决定](../../.agents/decisions/development/README.md#d-20260907-agents-writing) 的负责范围与索引；此时核对两份 AGENTS.md 相对基线均无差异。随后修改根入口、补齐决策目录符号链接，同步文档维护、结构图与 CHANGELOG。所述范围落实并核对后，两份决定在本工作分支进入 implemented。

## 核对结果

| 对象 | 方法与结果 |
| --- | --- |
| 独立书写归属 | 核对两份决定的受管文件表和指令回链：新决定只负责根 AGENTS.md，原决定只负责决策目录 AGENTS.md；项目 16 份决定 ID 无重复 |
| 第一项必要规则 | 根入口第一节为必要规则，明确每份 AGENTS.md 都有同目录 CLAUDE.md 相对符号链接，新增、移动和删除时同步维护 |
| 符号链接 | 枚举项目 AGENTS.md，使用 Python 的 `is_symlink`、`readlink`、路径解析与逐字节读取比较；根目录和决策目录两处 CLAUDE.md 均指向同目录 `AGENTS.md`，目标存在且正文一致；Git 索引中两者的模式均为 `120000` |
| 仓库布局 | 根入口为 53 行、3,252 字节；核对 17 个实际路径全部存在，两个产品目录明确标为“待确定，尚未创建”；未建立的运行、构建和测试命令如实说明 |
| docs 入口与团队约定 | 根入口按任务引用开发、Issue、文档和版本指南；除自身归属回链与局部 AGENTS.md 入口外，其余 Markdown 链接均指向 docs；团队约定单独成节 |
| 当前说明一致性 | 文档维护指南已说明独立归属、同目录链接、布局更新和开发框架操作内容进入 docs；结构图补齐决策目录 CLAUDE.md；原有 PR 决策增量等开发要求继续由指南承载 |
| 局部指令与原始资料 | 相对基线，决策目录 AGENTS.md 正文及 materials 均无差异；本轮只补齐局部指令的符号链接 |
| 链接与格式 | 完成目录迁移并纳入本记录后，检查 45 份正式 Markdown 的 225 处相对链接与锚点，均有效；`git diff --check` 和 `git diff --cached --check` 通过 |

根入口占位只表示当前缺少对应实现，不规定未来产品源码或测试的实际路径。用户截图用于参考“路径与职责”的呈现形式，未采用截图中的外部项目模块。

## 验证边界

本记录证明本工作分支中文件、链接、归属和当前说明一致。未启动新的 Claude 或 Codex 会话做加载实验，未验证两种工具的全局配置或执行效果一致，未验证长期协作收益。当前没有产品运行命令，因此没有产品测试结果。

本轮未进行其他开发者审阅；根入口的已确认改动与 Issue #20 的其余研究问题分别验收。没有产品版本范围调整，Roadmap 不变；可见的协作入口变化记录在 CHANGELOG 的 Unreleased，不代表已发布。
