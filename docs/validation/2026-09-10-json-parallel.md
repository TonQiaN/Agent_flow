# JSON Map/Fork 作者验收

2026-09-10（悉尼）；主负责开发者 @xiaoxuanli-a，Codex 实现与作者自查，关联 [Issue #16](https://github.com/TonQiaN/Agent_flow/issues/16)。预检基线 98c6f56，复用 #13 持久化、#14 队列和 #15 节点重试。没有独立审阅、远端 PR 发布或正式 Issue 关闭。

## 预检与实现边界

先查 Blackbox v0.1.22 / 5610d1b 的 parallel-execution.md、Map/Fork 定义、test_parallel_runtime.py 及并行修复历史。采用稳定 ID、Map 原索引、Fork 字典序、共享容量及部分结果保留；只实施用户确认的单层 JSON、单 Component 与 wait-all，不迁移旧配置、fail-fast 或旧预算重置行为。旧 Python 测试没有在本次运行。

新增 ParallelWorkflowCatalog 与纯展开/汇合模块；每项关联普通单节点检查点，在同一 RunRecordStore 中经现有 Worker 执行。父展开、子项初始记录和队列引用共用一笔条件事务。父等待释放占用，子项全部结束后由队列唤醒父节点；正常输出契约、路由、接纳和 CAS 继续生效。结构层不依赖 Docker、Harness、秘密来源或业务评分代码。

最大 62 项来自现有原子记录接口的 64 条条件限制；空 Map 合法，Fork 至少两个分支。队列索引更新至 v3，不支持开发 v1/v2 自动迁移。对业务而言没有嵌套子流程接口；内部单节点计划是复用现有恢复与调度接口的执行记录。

## Issue 验收对照

| 条目 | 实际证据与结论 |
| --- | --- |
| Map 身份与顺序 | 故意按 B、C、A 完成，结果仍为 A、B、C，保留 index。非数组、重复/空白 ID、项目契约错误在展开前失败；空数组产生空 items。 |
| 固定 Fork | 同一逻辑输入交给 alpha/zeta，按 ID 字典序汇合；非法分支、输入 ID/schema 不一致在登记时拒绝。公开 Fork 示例输出 alpha=14、zeta=21。 |
| 契约与范围 | 验证逐项输入/输出和最终汇合；坏汇合 contract 没有后继。文件、Effect、嵌套、额外字段、非法容量和非 wait-all 策略明确拒绝。结构层不解析 Agent 日志。 |
| 调度与隔离 | 组上限、共享角色与认证上限分别进一步收紧容量；父任务 owner=null 时等待。独立进程竞争仍不超组上限。真实 Docker Script 每项使用不同 Runner 目录、可写输入副本及独立 outputs，原始输入不变。 |
| wait-all 与取消 | B 最终失败后 A/C 仍完成，父保留 B 的定位诊断且不运行 finish。未启动项直接取消；活动项协作取消后父才结束。重试项在领取期间被取消，不创建新 Attempt、不遗留 retry 元数据。 |
| 恢复与重试 | 展开事务故障不留下部分子任务；SIGKILL 父展开进程后恢复只保留一组。A 已接纳后杀死 B，恢复保留 A/C，B 用 Attempt 2；普通失败后重新打开 SQLite 也只重试 B，父展开身份不变。篡改子 Run 引用被严格加载拒绝。 |
| 解耦 | engine/parallel 只使用契约、检查点和记录端口；队列负责稳定记录键及原子写入，现有 Worker/Runner 处理执行及资源。实际依赖边界检查通过。 |
| 文档与交付 | [指南](../guides/json-parallel.md)和[可运行示例](../../src/examples/json-parallel.mjs)已提供；本地改动包含实质决策正文。作者验收已具备，PR 提交、审阅、合入核对尚未完成，不能关闭 Issue。 |

## 执行结果与修正

本地 Node 26/macOS：构建、测试类型检查及依赖边界检查通过。Map/Fork 公开示例通过：Map 顺序为 a/b/c，结果 2/4/6；Fork 顺序 alpha/zeta，结果 14/21。

最终普通测试 404/404 通过，20.67 秒，零失败、取消或跳过。包含并行登记、队列集成、实际独立进程中断，以及新增心跳竞争回归。六项相关 Docker/Agent 测试 6/6 通过，56.76 秒：一个实际 Agent 重试、一个 JSON Script 并行隔离、两个 Worker SIGKILL/查询故障恢复、两个 Attempt 2/3 SIGKILL 的有限重试。后两项验证预算没有变成 Attempt 4。

最后自查修正正常任务释放归属后迟到心跳错误触发取消的问题，并为其添加有控制的竞态测试；同时避免取消发生在准备子记录期间仍发布新并行项。早期新 Docker 夹具的脚本括号与 AJV 严格 schema 声明错误已修正，没有放宽产品契约。首次最后回归同时启动普通与 Docker 套件，两个普通队列测试文件持续等待，人工终止；具体触发点未证实，不能计为通过。单独运行这两个文件 30/30 通过，心跳竞态单测通过；Docker 结束后完整普通回归加入每测试 30 秒超时并独立运行，取得上述 404/404 结果。后续普通与 Docker 验收按阶段运行，避免共享宿主负载影响短租期测试。

## 证据局限

Docker 测试使用合成 JSON、凭据与协议替身；JSON Script 的逻辑结果转换器是隔离验收夹具，未新增开箱即用的 JSON Agent 转换适配器。已有 Agent outputs 接纳协议未变；本次没有官方模型登录/调用、真实学生卷、评分质量、完整报告或 PDF 验收。没有机器断电、生产部署、历史迁移或 Node 24 远端 CI 结果。单机数量上限、非公平扫描及资源未知时阻塞的既有边界仍适用。

产品取舍见 [P-20260910-json-parallel](../../.agents/decisions/product/README.md#p-20260910-json-parallel)。

## 短栈交付前复核

最终本地2053dc9切片同步已提交的队列和后续重试分支；产品语义保持一致。包边界、构建、测试类型及全部普通测试407通过、0失败、0跳过（22.46秒）。六项并行/实际Agent重试/Worker恢复Docker联合检查通过、0跳过（40.15秒）；当前修复CI镜像准备，尚未把本切片提交为第四层远端PR。

Tutor消费端使用已安装业务校验器和渲染器完成20项联合回归，0失败、0跳过（171.57秒），包括正常报告、返修、来源和复核篡改、缺失确认与错误摘要拒绝。所选真实学生的已保存批改/复核/报告链另做只读回查：16张原始作答未变、35项、7项经用户确认的缺失作答，报告45/70、18页PDF；PDF摘要与既有视觉验收相同。未重复调用模型，不将合成20项冒充新的真实学生实跑。


## 恢复后凭据等待的整组取消

在本地 89b5af4 复核 #16 时发现：#14 已允许取消清理完成后重新等待凭据的串行任务，但整组取消仍只检查子记录的历史 currentIdentity。先核对 Blackbox 5610d1b 的 runtime.cancel_run 和 storage.cancel_dispatch：待调度取消与活动执行的协作取消分别处理，记录与调度变更共同提交。这里复用本项目既有恢复完成事实及条件事务，不复制旧整 Run 调度或单凭租约到期认定停止。

用真实 Worker 子进程执行 Map：A 已接纳，B 执行期间 SIGKILL，恢复确认旧执行清理后通过合成准入端口返回凭据繁忙，C 尚未执行。修复前两个用例均在取消父组时抛出 QUEUE_TASK_ACTIVE。修复将串行和组内未领取项的停止判断归入同一私有函数；不新增状态、配置或授权接口。

修复后两个回归通过、零跳过（6.08 秒）：

- 清理完成的 B 可取消，严格加载保留原一个 Attempt，父组结束为 cancelled；不执行 C 或正常后继。补充将清理证明置为未完成的端口负对照，仍拒绝取消且原记录不变。
- 在取消事务提交前允许另一个 Worker 重新领取 B；原提交因版本冲突重新核对，保留新领取令牌及原检查点，仅设置协作取消，父组继续等待。

构建、测试类型、依赖边界及完整普通套件已执行：407 项中 403 通过，4 项既有 CONNECT 测试因执行沙箱禁止监听 127.0.0.1 报 listen EPERM，无跳过。随后在允许回环监听的环境只复核该代理文件，5 项全部通过（含上述 4 项），未改测试或产品逻辑来放宽检查。原失败日志保留，不能称首轮 npm run check 全绿。默认 E2E 为 17 通过、188 跳过；本次没有启用 Docker、官方模型或私有消费端验收，未将跳过计为通过。

本修复涉及队列取消和原子归属，新增证据来自真实进程中断与 SQLite 事务；先前 Docker、真实学生及报告证据仍绑定各自原版本。完整并行切片审阅、远端提交和依赖合入仍待完成。

## 显式重试配置的登记校验

在本地 572bfc2 继续作者审查时，发现 Map/Fork 通过真值判断转交 retry，导致显式 null、false、0、空字符串被静默丢弃。先核对 Blackbox 5610d1b 的 loader.py 重试策略解析及 project-manifest.schema.json：schema 要求对象，loader 复用可选 mapping 和次数校验；没有据此声称旧 loader 拒绝 null，也没有运行旧 Python 测试。本项目复用 #15 已有严格策略校验，只将两个转交点改为检查属性是否存在，不增加第二套校验器。

修复前两个新增用例（Map、Fork）均在首个 null 输入上复现 Missing expected exception。修复后完整验证四个无效值、失败登记不保留子计划/名称，以及合法策略原样传递。构建、测试类型和依赖边界通过；登记、并行队列、实际进程中断/取消和重试四个相关文件共 29 项通过、0 失败、0 跳过（16.73 秒）。此次针对两处策略转交修复运行受影响测试，未重跑完整套件、Docker、官方模型或私有学生验收；既有证据仍绑定原版本。完整 84 文件切片审查尚在继续。

## 整体切片的默认行为复核

继续检查并行、共享执行组合及 Tutor 消费端的完整切片。对照 retry 基线 12676d8，普通串行、持久串行与队列串行使用相同有效/无效输入，结果与持久 Run 内容完全一致；未配置图片和空图片数组的 Codex 单/多 outcome 计划均与基线一致。队列索引从开发 v2 改为 v3 是已说明的整体格式变化，不宣称旧索引无需迁移。

默认 Docker 配置对照发现：虽然输入上限仍为 256 MiB，但新增 maxInputBytes 默认字段改变了执行定义；严格恢复比对可能因此拒绝原任务。先限定检查 Blackbox 5610d1b 的 runtime/storage/loader，未找到本项目这种执行定义指纹机制；实际修复复用本项目 DockerBackend 的唯一配置序列化入口。默认值沿用旧隐含表示，非默认预算仍显式记录。新旧实际已固定本机镜像的完整 definition 对照通过：省略和显式默认均与基线相同，非默认预算仍不相同。无需放宽恢复比对、添加迁移器或改变复制限制。

预算修复后构建、测试类型及 Docker 文件配置测试通过。另显式 Docker 组合 6 项全部通过、零跳过（39.40 秒），覆盖准备失败的认证释放、并行 JSON Script 独立空间、Worker 被杀及查询故障恢复、Attempt 2/3 被杀后预算不重置。20 项显式 Tutor 合成消费端测试全部通过、零跳过（176.50 秒），覆盖实际安装的业务 Gate/渲染、独立替身角色、返修/拒绝、来源保护和用户确认缺失投影。Tutor 代码未修改，测试没有读取学生原件或调用官方模型；不将这些替身测试作为模型评分准确性证据。

两处修复后的最终 npm run check 通过：依赖边界、构建、测试类型检查、409 项普通测试（27.52 秒，零失败/跳过），默认 E2E 17 通过、188 按环境跳过（10.64 秒）。另核对 789 个本地文档路径链接，无缺失。完整 84 文件切片已完成本地作者审查，覆盖并行引擎/队列、共享图片/预算/收尾及消费端和文档，未发现剩余阻塞代码问题；没有独立评审或本切片远端 CI。PR 仍按依赖和最多三个未合并功能 PR 的顺序提交，不提前关闭 #16 或宣布版本发布。

## 2026-09-11 结构测试的时钟隔离

PR #31 的一次 Node24 CI 在 Fork/空 Map 展开断言中没有取得 PARALLEL_WAIT。夹具给 Worker 选择最短300毫秒真实租约，结构展开需要多次存储提交；调度停顿会合法触发 QUEUE_CLAIM_LOST。先核对 Blackbox scheduler 的租约心跳与失去所有权边界，再复用本项目 queue/retry.test.ts 已有的注入时钟方案，不修改产品租约或放行失效归属。

在打开节点前强制350毫秒同步停顿，旧夹具稳定复现 QUEUE_CLAIM_LOST。结构测试改为 Queue、Workflow 和 Worker 共用可控逻辑时钟，真实异步 sleep 仍保留；需要重试到期时显式推进10毫秒，重开沿用同一时钟。这个强制停顿保留为回归场景，修正后13项并行结构测试通过、0跳过。真实租约到期、失联、迟到写入、进程SIGKILL测试不改动，生产默认租约也不改动；结构测试通过不等于最短租约可承受任意宿主阻塞。首次CI失败保留，当前提交以重新运行的CI为准。
