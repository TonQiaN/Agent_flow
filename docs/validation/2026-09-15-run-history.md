# Issue #39 第一层：运行历史与日志

基线 d5d0cae，2026-09-15，macOS、Node 24.18.1。此记录只说明基础层，不代表前端或招聘流程整体验收。

`npm run check` 已通过：普通测试 419 项通过；E2E 17 项通过、188 项 Docker 用例按默认开关跳过。新增用例覆盖事务冲突无假历史、顺序与分页、重开、v1 迁移未知时间、真实普通流程快照记录，以及跨传输块凭据脱敏、中文完整性、截断和写入失败。既有 SQLite 跨进程与 SIGKILL 测试继续通过。

Codex 固定版本的真实会话归档及日志 UI 核对由 #39 后续应用和验收层完成。完整容器矩阵由 CI 启用 Docker 开关执行；配置存在不代表当前提交远端已通过。

实现为 Codex 作者自查，尚未其他开发者独立审阅。

## 合并前评审修正 · 2026-09-17

核对 PR #40 的三个未解决反馈，复现私有恢复字段进入展示投影、可选日志写入失败把成功 Harness 改成失败。改为公开字段投影与独立采集诊断；另按序列化后的 UTF-8 字节限制 Codex 会话 envelope。新增三个回归，与原历史/日志四个用例共 7/7，通过边界、构建与类型检查。回归验证私有记录不被修改、Prompt/容器公开参数保留、日志错误仍可见但 outcome 不变，以及大量反斜线的会话保持有效 JSON 并标记截断。

参考 Blackbox 固定提交 `4dc0f4a` 的 execution_plans/collector/artifacts 与 audit-session；其 Python 归档接口没有本项目 Runner/日志接收端的对应接线，修正按本项目既有查看与接纳分离规则完成。首次回归日志保留于本地，远端结果在当前 PR 续接。
