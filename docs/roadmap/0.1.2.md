# 0.1.2：持久恢复与单机任务调度

## 目标

在相同定义和环境下恢复中断流程；多 Run 通过单机 Worker 共同遵守角色与认证容量。

## 范围与非目标

#13、#14。节点边界恢复、持久队列、单机多进程 Worker。暂不做会话接管、历史环境重建、多主机、抢占或复杂公平调度。

## 版本验收

满足 Issue 详细验收；对批卷夹具注入引擎/Worker 中断、取消和多 Run 竞争，验证已完成初批不重跑、旧执行未停不启动副本、容量与结果归属正确。

## 关联工作

预计两个功能 PR：持久化与恢复 → 队列与 Worker，联合验证后完成本版。

- [#13](https://github.com/TonQiaN/Agent_flow/issues/13)、[#14](https://github.com/TonQiaN/Agent_flow/issues/14)

## 当前进展与剩余验收

#13 首版实现已完成作者验收，包括 SQLite/CAS、耐久文件归档、结构/执行定义、严格检查点加载及共同 Runner 的节点边界恢复。已接通 API key 与订阅 Agent、确定性函数、指定 JSON 文件投影和固定 Effect；合成批卷覆盖原始来源重开、用户返修、已接纳节点保留及未知发布阻塞。逐项证据、实际版本与失败记录见 [#13 验收对照](../validation/2026-09-10-issue13-acceptance.md)；当前能力边界见[恢复指南](../guides/workflow-recovery.md)。

持久化 PR 的完整审阅与合并仍待完成。本分支尚未包含 #14 队列/Worker 的交付；该项将通过独立 PR 增加 NodeTask 调度、共享角色/认证容量及 Worker 失联恢复，联合验证仍按上面的版本验收要求执行。

基础认证的真实官方矩阵、真实学生批卷与报告/PDF 是整个交付目标的独立验收，不能用此处的协议替身恢复实验代替。首版不增加通用崩溃 GC、任意宿主文件闭包恢复或旧环境迁移作为前置；未知启动/来源操作与 Effect 结果继续明确阻塞。

0.1.2 尚未发布；本地作者验收、PR 交付与发布状态分别核对。历史切片进度保留在对应[验证记录](../validation/README.md)，不作为当前规划的未完成清单。
