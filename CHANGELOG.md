# Changelog

这里记录实际变化；未来目标见 [Roadmap](docs/roadmap/README.md)，维护方式见 [版本维护指南](docs/development/versioning.md)。目前尚无正式发布版本，Unreleased 不代表已发布。

## Unreleased

新增显式版本化的确定性 JSON 函数登记：配置和函数同时捕获，支持持久启动、版本比较和中断节点重算；已接纳 Gate 不重跑，迟到结果由 Run CAS 拒绝。普通函数保持原用法。

固定 target/key 的 apply Effect 已接入实际执行快照、严格回执加载和 Workflow 恢复。已完成节点保留，已提交回执复用；pending 在认领前阻塞，新 Attempt 仍需当前授权。动态映射与 dry-run 尚不支持持久启动。

新增可选 Effect 持久操作日志：唯一占位提交后才调用服务，回执耐久后才接纳；新进程可复用已确认结果，未知操作拒绝重发。仍需本次显式授权，Workflow 恢复接线尚未完成。

内置 ArtifactStore/Archive 新增宿主直接物化捕获：在目标存储内校验并接收副本，省掉 Catalog 检查点/恢复目录及重复复制；归档仍按原耐久发布顺序提交，临时快照释放整个本次暂存范围。旧存储端口保留路径回退。

同存储 Catalog/Agent 输入改为借用已登记快照，省掉重复物化目录和输入捕获；不同存储保留回退。停止不明时继续占用输入，清理成功才允许释放；恢复的物理快照 ID 与历史收据内容保持关联。

版本探针和 Agent Driver 的输入副本改在已登记的 Runner 工作目录内创建，失败与恢复释放统一清理。新增宿主物化能力和空输入，保留原有复制校验；Driver 选项移除 inputRoot，仅保留 timeoutMs。其他宿主临时目录的崩溃收尾仍待完成。

新增不可变 API key Agent 的 Workflow 阶段接线和严格文件收据恢复；凭据获取前后等待 CAS，失败阻止执行，恢复先清理旧资源再进入新 Attempt。DeepSeek 合成协议通过真实容器中断验证，订阅占用和宿主临时目录崩溃收尾仍待完成。

新增 Workflow 有序资源/宿主操作阶段：与正常执行共用 CAS，恢复先清理所有旧阶段资源，再重跑当前节点；未知宿主操作拒绝接管。检查点升级 v5、执行快照升级 v2。完整 Agent 接入仍待完成。

- 不可变环境凭据的执行资源可通过共同 Runner 恢复清理，源密钥轮换或删除不会被回滚；新增分开的探针/执行记录端口，完整 Agent Workflow 与订阅恢复仍未完成。

- Agent 版本探针可独立保存资源和启动记录，恢复通过共同 Runner 清理；阶段完成记录失败时阻止获取凭据。完整 Agent 恢复仍未完成。

新增无私有认证的 CONNECT Script 恢复：固定代理程序与镜像，核对完整资源身份，确认任务容器、代理和内外网络都移除后再恢复新 Attempt；部分清理失败保留重试能力。

新增实际 Agent 执行定义快照：保存用户说明、Harness 计划/版本、运行资产及非秘密 Profile，固定执行和代理镜像并用于后续运行；定义读取不访问凭据或启动容器。Agent 资源和认证恢复仍待完成。

新增 Workflow 恢复 CAS 认领与旧资源共同清理，保留原业务快照；并发恢复及旧宿主迟到写入不能覆盖新 revision，查询/停止/释放不明可重试。只读加载同时支持恢复记录，不产生新认领。断网脚本的新 Attempt 恢复已接通共享执行循环：保留中断历史和已接纳产物，同一 NodeTask 递增编号，旧 writer 的迟到提交拒绝。

Workflow 检查点为 v5：正常 Attempt 保存身份、Runner 资源、结果步骤关联及启动操作进度；资源保存和每次准备/创建/启动前等待 CAS，启动完成必须由实际观测确认，失败阻断后续操作。每次调用的端口拒绝跨身份、重复、重叠和迟到保存；严格加载器拒绝旧未发布 v1/v2/v3/v4 试验记录，不自动补造证据。恢复认领见上，新 Attempt 恢复见上。

新增 Runner 实际资源保存和重启后的查询/停止/移除端口：保存失败阻止创建启动，完整身份或环境不匹配拒绝，停止不明保留资源；断网脚本 Workflow 恢复见上。

新增严格检查点加载和独立文件引用恢复，核对实际定义、历史、归档和收据并提供失败回滚；修复归档完成时取消可能写入值/步骤不一致记录的时序。

新增正常 Workflow 检查点写入：共享执行和路由、初始/接纳文件归档、状态 CAS 与持久取消确认；写入失败阻止后继。

新增断网脚本执行绑定快照与 Docker 镜像冻结，执行消费同一镜像 ID。

新增实际 Workflow 结构与契约快照，支持跨 Catalog 同名定义一致性核对。

新增独立耐久文件归档、带摘要引用及跨进程可写物化；临时句柄释放不会删除归档。

新增独立 RunRecordStore 与私有 SQLite 状态保存、版本 CAS 和损坏拒绝。

新增三种 Harness 共用的合成批卷验收入口及离线预检，复用同一文件契约、Gate 和用户返修；真实官方矩阵仍待完成。

新增私有备份与 auth recover：仅恢复同一已提交身份/版本的尾部截断，删除和替换先清除旧备份，拒绝回滚健康文件或恢复已删除登录。

新增 Codex/Claude 终端登录和本地检查/删除：隐藏授权码输入，取消、超时与清理协调；清理不明保留占用并返回独立状态，真实账号登录尚未验收。

接入 Codex/Claude 原生订阅登录驱动、私有 Docker 输入输出和失败清理重试；先断网核对版本，拒绝将 Claude 失效标记当作新登录，真实账号登录尚未验收。

新增订阅登录协调与首次配置管理租约：登录占用跨进程排他，失败或停止不明保留清理责任；原生登录驱动已由后续切片接入。

新增 DeepSeek 本地认证命令：隐藏终端录入、受控文件导入、检查和本地删除；状态不冒充远端认证有效。

DeepSeek API key 改为独立环境绑定：Docker 命令只传变量名，执行目录不再创建密钥文件；普通任务配置和订阅文件刷新方式保持既有边界。

- DeepSeek 增加固定容器启动程序，从受检私有文件向 CLI 环境交接密钥，失败或取消不采集成功会话；图像字节交接与保留代码入口拒绝已由真实 CLI 验证，宿主认证组合仍待接通。

- DeepSeek 增加私有原生会话采集与内部完成证据解析，核对任务/事件/工具配对并避免用量重复累计；Runner 原始记录共享原有 16 MiB 总预算，单项可使用全部预算。完整执行组合仍在开发。

### 修复

- 修复 DeepSeek 文件操作取消后后代持有管道导致队列无法结束：固定文件 worker 使用专属进程组终止并等待 close，保持文件隔离与 Runner 停止确认。

- 修复 Claude2.1.226 未实际加载任务管理策略的问题：移除无效路径环境变量，经宿主受限配置映射挂到固定系统位置。新增真实 CLI 工具负对照与隔离回归，验证凭据拒绝、路径绕过、正常副本写入和 Bash 网络阻断；没有使用真实凭据或远端模型。

### 新增

- DeepSeek 受控联网 Runner/AgentDriver 与应用资产交接已接通：同一不可变镜像预检、静态 key 快照、具名原始会话和输出契约共用执行收尾。Codex/Claude 保留公共入口并复用统一凭据执行层；真实 DeepSeek 调用仍未验收。

- DeepSeek 增加静态 API key Codec/Profile 和快照脱敏；执行绑定支持短租约读取后的独立副本，不把 API key 全程串行化，不回写覆盖管理轮换/删除。宿主存储到原生 CLI 的合成交接通过，完整联网入口仍待完成。

- 新增纯 DeepSeek Adapter 和具名会话证据接口；多出口经原生工具成功 metadata、用户枚举及正常终态共同判定，选择后继续调用工具不接纳。关闭自动标题模型，辅助模型或重试用量不能完整确认时保留未知；宿主认证执行组合仍待完成。

- DeepSeek 容器文件服务通过原生接口隔离 I/O：固定输入副本和输出可写，私有状态/配置受保护，保留原生版本与原子写入；Bash 与搜索现共用隔离进程层和临时目录，保留原生超时、后台、取消与有界输出；完整执行组合仍待接入。

- DeepSeek 内部配置映射与实际 CLI 断网预检：严格配置、原样任务文本、工具开关及私有原生会话；记录默认策略无法满足固定目录的验收缺口，尚未提供执行组合。

- Claude 订阅 codec/Profile、独占执行绑定与 Agent 文件交接已接通；复用订阅执行收尾，保留 provider 独立映射。识别明确无效 token 清除状态、拒绝隐式复活；凭据文件支持安全隐藏名称，文件权限规则明确锚定绝对路径。通过合成执行和真实 CLI 断网格式识别，真实调用仍未验收。

- 新增独立 Claude 2.1.226 Adapter：严格参数与固定任务路径、结构化 outcome、同 session 终态与 is_error 检查、脱敏事件及不重复的 usage 映射；真实无凭据断网启动通过，真实订阅调用和工具隔离仍待验收。

- 批卷应用支持宿主注入真实或合成 Agent 驱动、任务绑定和路由定义；真实 Codex 已通过合成试题的正常批卷与一次返修流程，独立输入可修改、原始材料不变，发布保持 dry-run。

- 新增显式文件到 JSON Transform、私有转换收据及 Tutor 合成批卷样例：按来源、覆盖、评分和证据校验，用户定义返修目标/次数，Gate 来源与转换结果复核后模拟发布；不同来源内容不能复用同一操作键。

- 新增独立模拟 Effect 和 JSON Workflow 接入：默认 dry-run、绑定请求与执行身份的一次授权、独立业务凭据、幂等复用和冲突；结果未知时保留占位并阻止重发，取消不冒充回滚。

- Workflow 新增确定性脚本适配：stdout 单份结构化 outcome、stderr 日志、outputs 契约产物，通过既有 Runner 确认退出和清理；真实 Docker 文件 Gate 交接、异常协议、取消与超时通过验证。

- Workflow 接入可信文件 Gate/Transform 与 AgentExecutor：私有引用绑定 Run 和前序，独立可写输入在交接时复核摘要，执行资源清理失败阻止推进并保留可重试能力；新增跨 Agent/Gate/Fixer 的合成集成验证。

- 增加独立 Workflow 编译器与串行 Run 控制，先接入 JSON gate/transform 函数；校验契约类别/ID 与出口路由，支持用户定义的返修上限、最大步数、查询和合作式取消，保留最后接纳结果与停止未证实的失败。

- 增加与 Harness/认证解耦的 Agent 接纳层、Codex 执行驱动及进程内可信收据：绑定身份、输入输出快照与 outcome，拒绝伪造/跨 Run 前序和重复 Attempt，清理恢复不会升级原失败结果。

- 文件契约注册与本机快照适配解耦：自动发现 outputs、校验路径/树/数量/大小/类型及 JSON Schema，拒绝歧义和未归属内容；交接重新校验摘要并生成独立副本，失败清理半成品。已接入真实 Codex 小任务，可信前序记录已接通，完整 Workflow 仍待完成。

- 增加 Codex managed ChatGPT codec、明确 Profile、同镜像版本预检、已知凭据值脱敏及订阅执行组合入口；本地、合成 CLI 连接与获授权的真实 Codex/gpt-5.6-sol 数字任务通过，输入副本可改且原件不变；通用文件契约已接入，真实刷新与 Tutor 流程仍待验收。

- 新增独立私有凭据执行绑定：单次副本、停止和清理后条件回存、失败保留租约，以及收尾前禁止释放工作区。合成刷新已在真实 Docker 的非零退出、取消和超时场景验证；真实刷新仍待验证。

- Docker 新增可选 CONNECT 代理：每次执行独立网络、精确公网 IPv4 目标、禁用直连及外部 DNS、有界转发与共同清理；真实 TLS、拒绝路径和故障清理已验证。已接入首个 Codex 订阅组合。

- Docker 支持只读协议配置文件注入及宿主选择的 nested-userns-v1 策略；真实 Codex 沙箱验证了可写任务副本与认证文件访问拒绝。已接入首个 Codex 订阅组合。

- Harness 显式注册、Codex 0.153.4 调用计划和结束后事件/终态 parser；新增私有凭据存储、跨进程独占租约与 generation/revision 刷新检查。真实认证 Runner 组合见上述首个订阅入口。

- 离线 Docker Runner：每次执行可写独立输入副本、统一任务路径、流式有界 raw 采集、实际停止确认、容器清理和显式工作区释放；新增真实 Docker 验收。认证和联网通过独立集成层接入。

- 根 src 下的 TypeScript 工作区、严格编译、依赖边界检查与 Node 测试/CI 配置。
- 确定性 gate/transform Component、独立定义与实现注册、Run/NodeTask/Attempt 身份、严格 JSON Schema 契约和输入/结果副本；CLI 提供可运行 demo。该确定性入口不包含 Workflow 或 Agent 执行。

- Issue 表单扩展为功能、缺陷、研究、决策、维护五类；加入交付物选择、续接记录和各类专用字段，支持仅交付正式决定的研究/讨论，并提供填写指南和边界示例。

- 建立 Roadmap 总览与按需版本计划方式，区分版本安排、任务进展和实际交付。
- 增加变更记录及手工发布维护指南，明确发布 tag、维护职责和 PR 中的同步时机。

### 修复

- Codex 0.153.4 对固定临时 input/work/outputs 显式允许元数据子目录写入，避免不适用的默认只读挂载；新增真实离线启动与元数据写入/认证隔离回归。

- Codex 初始化警告可出现在 turn.started 之前；明确的 turn.failed 作为失败终态处理，避免误报事件顺序或缺失终态。冲突和终态后的事件仍拒绝。

- 文件收集参考 Blackbox 忽略未归属的普通空目录，避免 Harness 工具留下的空目录阻止合法交付；未声明文件、危险链接和已声明目录树仍严格验证。
