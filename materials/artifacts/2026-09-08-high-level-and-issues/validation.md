# Artifact 验证记录

日期：2026-09-08。验证对象是本目录的讨论材料和阅读页面，不是 AgentFlow 产品能力。

## 后续三点补充修订（U2）

- 回放范围收窄为编排器、Runner 与 Harness 调用边界的时间、状态、输入输出和产物；删除内部消息/工具事件采集、原始 agent 日志定位等旧要求。运行中的回放只显示外围状态与经过时间。
- 镜像环境与节点 Harness 能力配置分别定义、快照和展示。节点任务描述/边界之外可设 skill、MCP 等开放能力；前端第一版仍只读。
- DSH、Codex、Claude 各有独立配置与调用模块，分别讨论运行指令文件和 DSH 插件机制；具体接口与生效行为列为未来验证内容。
- 共 19 项候选同步修订范围、产物或验收并标记 U2 来源；总数仍为 25 项，保留原计划 8 项、额外 17 项。本次未增加候选编号、创建实际 Issue 或修改正式决定。
- 重新构建 HTML、Markdown、JSON；逐项核对标题、范围、产物、决策归属、待讨论事项和验收文本一致，依赖图无循环。JavaScript 语法检查通过。
- 检查当前页面 247 处本地链接/锚点，均存在；没有重复 HTML ID 或外部运行时资源。来源核对记录中的 8 份原始材料/仓库文件摘要保持一致。
- 本次未重新读取飞书聊天或云端转写，未重做浏览器交互实测；CSS/JavaScript 未改。以下浏览器结果属于初版验证，不能视为本次重测。当前静态结果见 `sources/static-checks.json`。

## 初版来源与内容验证

- 飞书私聊实时读取成功，27 条、1 页、分页完整；25 条项目相关消息与原归档比较无新增或正文/编辑/删除状态变化，最新消息为 12:34 合并版。
- 原始会议 TXT 直接读取 619 段；会议来源、导出日期、两种时长表示和 SHA-256 记录在 `sources/verification.json`。本轮没有刷新云端逐字稿或读取 AI 摘要作为依据。
- 原 8 项计划全部保留；额外 17 项显式标记。25 项均含目标、范围、交付、主责待确认、依赖、来源、决策归属、待讨论问题和候选验收。
- 检查全部依赖引用和来源编号；依赖图无循环。E14 的 0.1.3 子范围明确另依赖 B07/B08；E15 的评估设计与实际实验时机分开。
- 对照用户六项要求的覆盖表已放在正文。建议版本号、环境选型和细化验收都标为待讨论，没有把研究结论或产品能力写成已实现。

## 初版构建与静态检查

- `python3 materials/artifacts/2026-09-08-high-level-and-issues/build_artifact.py` 成功生成 HTML、Markdown、JSON，三者同源。
- `node --check materials/artifacts/2026-09-08-high-level-and-issues/interactions.js` 通过。
- 检查 224 处本地链接/锚点，文件与目标锚点存在；没有重复 HTML ID。
- 页面 CSS 与 JavaScript 内联，没有外部运行时资源、字体或网络请求逻辑。原始资料链接按需访问本地材料或飞书，不属于加载依赖。
- 当时的链接数量为 224；`sources/static-checks.json` 现保存 U2 修订后的最新静态检查结果。

## 初版浏览器实测

通过仅绑定 127.0.0.1 的本地静态预览，在 Codex 内置浏览器完成：

- 首页排版截图检查：标题、信息卡、讨论边界、侧栏和来源入口可读。
- 全部 25 项；原计划筛选 8 项；额外筛选 17 项；额外 + 前端筛选准确显示 E09–E14 共 6 项。
- 搜索 Container 能缩小范围；不存在的搜索词显示 0 项和空态提示。
- “展开当前结果”展开对应 6 项。筛选无结果时点击 B03 链接，清空冲突筛选、显示全部 25 项并打开 B03。
- 实际点击 42:27–45:28 原文链接，落到 `t-42-27` 下付方圆“一个节点……大节点……小步迭代优化”的发言，未落到重复时间的短应答。
- 检查中修正了额外筛选选项的 HTML 闭合错误，并重新验证通过；同时修正最后一项折叠卡误包含下一节锚点的问题。

限制：浏览器 URL 安全策略拒绝直接导航 `file://`，因此没有完成直接离线打开的浏览器实测，也没有绕过该限制。无外部依赖是静态确认；移动端、打印效果未做浏览器实测。未开展产品、真实 Harness、Container、恢复或并发实验；这些仅是候选 Issue 的未来验收内容。

## 改动范围

初版新增本 artifact 目录；本次 U2 仅在该目录修订内容并保留补充原文。既有聊天和逐字稿不改写；未创建 Issue/PR、发布版本或更新正式决策/模板，当前仍为讨论稿。
